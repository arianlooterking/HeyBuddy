# Optional override for isolated development/test data. Never point tests at real user data.
# CLICKY_DATA_DIR=C:\Temp\ClickyTestData
# API keys are entered in the app and stored with Windows DPAPI, never in this file.
