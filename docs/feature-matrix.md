# HeyBuddy feature matrix

Reference: supplied HeyClicky 1.0.48, build 57; inspected September 3, 2026. The Mac installer is a behavioral inventory, not executable Windows code. [Original MIT reference](https://github.com/farzaa/clicky) and [public changelog](https://www.heyclicky.com/changelog) describe different generations; private/cloud behavior cannot be inferred from the older source alone.

The current release is 0.2.3. Its shortcut/settings path has isolated 94-check coverage, and the compact input-reactive companion has 16 rendering checks. A separate eight-second probe reached local recognition through the selected USB microphone, though broad intelligibility testing remains open. The installed payload preservation baseline comes from 0.2.2; model-ready state, selected exact-name application activation, and approval-gated Notepad typing evidence come from 0.2.0 unless explicitly labeled. Authenticated account connectors, a live physical standalone-modifier shortcut, and the broader cross-application matrix remain open; historical or fixture tests are not presented as evidence for those flows.

| Area | HeyBuddy implementation | Evidence / remaining work |
|---|---|---|
| Companion | Half-size animated native pointer, compact input-reactive three-bar voice state, status/reply bubble, persisted follow/dock toggle, double-click expandable main window, tray, color/size/reduced motion | 16 rendering checks cover idle/listening states and 96/144/192 DPI. No copied Mac binary/assets. |
| Conversation | Typed chat, streaming selectable text, history, individually configurable one-button or combination shortcuts, tap-to-toggle and hold-to-talk voice input, focused Agent composer, provider selection and interruption; default Auto can answer or use tools, while Chat only has no tools | Real local Persian UI response persisted. Mode routing passes bounded fixtures. Tap/hold transitions, immediate persistence, Agent composer state, sided modifiers, ordinary keys, AltGr, exact-side dispatch, overlap protection, and repeat state pass 94 Settings checks; physical global gestures still need broader live testing. |
| Speech | Local multilingual Whisper small; EN/FA/TR Piper voices; device selection, previews, speed | Synthetic multilingual loops pass. One eight-second selected-USB-device probe reached local recognition with a quiet signal; broad human accuracy and echo/barge-in need testing, and Persian word errors remain. |
| Hands-free | Explicit opt-in VAD loop, voice-triggered interruption, continuous dictation, emergency stop | Implemented; no acoustic echo cancellation or persistent OpenAI microphone stream. |
| Screen | Selected window, monitor or region; original-resolution scribbles; normalized pointers/circles/rectangles/arrows; pause/resume/next and observed-click progression | Two physical monitors tested, negative origin covered, sketch pixel test passes. Mixed-DPI and real application walkthrough coverage incomplete. |
| Input | Windows UIA inspection, cached verified elements, constrained foreground text/click/key/scroll; ambiguity/elevation/protection refusal | Controlled fixtures pass. An installed Auto task typed the exact approved marker into a named Notepad document after a safe failed attempt and fresh snapshot; the executor verified target and outcome with no foreground change. A separate visual reread was not completed. |
| Apps & actions | Installed-app discovery and picker; exact single-app open requests in EN/FA/TR use the common runner without AI startup; registered app IDs only, with duplicate choices and existing-window activation | 18 non-interactive checks pass. Installed Auto requests brought existing Calculator and Telegram windows forward with one verified action each; no Telegram content was read. Broader launch, restore and cross-app input remain incomplete. No arbitrary executable/arguments/URL launching. |
| Dictation | Streaming preview, punctuation cleanup, dictionary, cancellation, frozen target, clipboard preservation, transcript history | Controlled Persian text insertion passed; Notepad/Chromium/VS Code/Office interactive matrix remains unverified. |
| Documents | Drag/drop/picker import; PDF/DOCX/XLSX/PPTX reading; bounded workspace read/search; generated text/Office/PDF | Real format round trips and OpenXML schema tests. Persian PDF rendered correctly through offline Edge print. Scanned PDFs need OCR outside this reader. |
| Research | Sourced public HTTPS reads, encyclopedia search, maps and public Polymarket data | Actual adapters, bounded requests and source URLs. No invented general search or authenticated browser scraping. |
| Memory/skills | Editable local profile and Markdown skills, enable/disable, path guards, backups | Persistence and traversal tests. No automatic unsupervised memory extraction. |
| Agents | Durable cards/history, follow-up/retry, bounded discovery, 30 actions/10 minutes, max two retries, exact previews; Auto uses the runner, and background Agent requires an actual successful tool action | Core policy/cancellation tests and real workspace file workflow pass. The installed Notepad run completed after five recorded actions and an explicit typing approval. Replies alone do not prove work was done; broader multi-application tasks remain incomplete. |
| Connections | Official MCP SDK, HTTP/stdio, OAuth/PKCE and refresh, protected secrets, tool toggles, tests and revocation | Transport fixtures and native setup UI pass. No real account access or approved test writes yet. |
| Local bridges | Obsidian reader, built-in Office document tools, setup for Office/Blender/Excalidraw/Codex/Claude MCP bridges | External bridge installation and matching application validation remain separate. |
| Cloud | Compatible, OpenAI, Anthropic, OpenAI Realtime text/image/tool/PCM adapters | Protocol fixtures pass. No paid/account-authenticated cloud calls performed. |
| Local runtime | Pinned downloads/checksums/resume, model removal, resource controls, authenticated hidden worker, CPU speech; optional startup preload defaults on with visible stages; bounded provider-client reuse | Real Qwen text/vision/tools and local speech run on this PC. 51 runtime tests and worker crash/restart pass. The installed UI reached Ready. Client retention improved; warm inference was not measurably faster. Deliberate GPU-exhaustion acceptance remains. |
| Recovery | Atomic settings/connector files, SQLite migrations/backups, interrupted task pause, secret protection | Unit/integration tests, real crash/history recovery, same-version reinstall preservation, and the 0.1.1-to-0.2.0 upgrade/data-preservation check pass. Restart recovery with an actual pending task remains unverified. |
| Release | 0.2.3 self-contained Windows x64 application, per-user installer, portable ZIP, checksums and notices | Package and installed-payload audits are required for each release. The build is unsigned, and the remaining interactive checks prevent a full-parity or production-readiness claim. |

Native iMessage, Find My and macOS automation are unsupported on Windows. Browser links are clearly labeled as browser access. This matrix describes functional coverage and limits; it is not a full-parity certification.
